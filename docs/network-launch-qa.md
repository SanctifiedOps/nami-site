# NAMI Cloudflare launch test register

## Live production validation (2026-09-23)

The Cloudflare cutover is complete. `namicreative.co.uk` and `www.namicreative.co.uk` now run through the production Worker. The verified live version is `7a5efef1-affc-4d6a-b7e0-d3186a84ddfb`.

The final local typecheck and Cloudflare production build passed. The live read-only safety suite passed all 26 checks, including public routes, invalid submissions, access controls, the independent ticket page, held events and every outbound integration gate. The production profile audit passed 185/185 published profiles.

Production D1 contains 185 members, 185 published profiles and 95 supplied profile images. It contains zero member invitations, zero member email jobs, zero support tickets and zero owner-alert jobs. The two previously recorded Sheet-sync jobs remain pending. No member email was sent or queued during deployment or validation.

The live member login page, password recovery and independent problem-reporting page are reachable. The login page includes a fallback link to the ticket route, and the tested mobile viewport has no horizontal overflow. The events noticeboard and Network News remain deliberately offline. The ticket, events and owner-alert frameworks are deployed, but events and owner notifications remain held until their owner controls and email delivery are approved and tested.

All member-communication controls remain locked: `OUTBOUND_EMAIL_MODE=hold`, `MEMBER_INVITATIONS_MODE=hold`, `OUTBOUND_EMAIL_RETRY_MODE=hold` and `EXTERNAL_INTEGRATIONS_MODE=hold`. `OWNER_NOTIFICATIONS_MODE=hold` and `NETWORK_EVENTS_MODE=hold` are also unchanged. Member invitations require Joe's separate, explicit approval after the remaining authenticated live dashboard check and planned profile improvements.

## Final autonomous pass (2026-09-23)

The full local typecheck and Cloudflare build passed. Staging passed all 21 route, validation, access-control and hold-gate checks. The isolated production-data preview also passed 21/21, live-versus-staging route parity passed 17/17, all 185 production member pages passed the profile audit, all six Sheet reconciliation tests passed, and the Make configuration audit passed.

The final production Worker was uploaded as version `d52258d9-fbc8-4b57-a671-85f3ce5aafc3` without deploying it. Its version preview passed the 21-check safety suite and 185/185 production profile checks. Binding inspection confirmed production D1 and R2, the NAMI Google service account, the production Sheet, Turnstile, Graph, Make and Mailchimp secrets, and all four launch holds. The live site, production Worker deployment and production DNS were not changed.

An authenticated staging member journey now passes login, dashboard load, profile read, invalid edit rejection, valid profile edit, invalid image rejection, profile image upload, portfolio upload, portfolio deletion, public-profile refresh, directory-card retention and public WebP delivery. The resulting Sheet job completed, and direct Sheet readback found exactly one row with the updated speciality, canonical Instagram URL, image key, active account status and successful sync timestamp. No member invitation or email job was created.

This journey exposed a migration issue before launch: most imported Instagram values are handles rather than complete URLs. The old dashboard validation would have blocked 175 production members from saving any change. Profile links are now normalized on read and write, including common malformed protocols and bare domains. The authenticated journey and the existing Joe dashboard both verified the fix.

Responsive browser checks passed at 360, 390, 430, 768 and 1440 px across Home, Contact, Creator Wave, Network, Directory, Login, Joe's profile and Joe's authenticated dashboard. No tested page had document overflow. Forms, cards and dashboard fields retained visible side padding on mobile.

Joe confirmed that the controlled Contact, Creator Wave and Creative Network test emails reached `hello@namicreative.co.uk`. The owner-controlled newsletter double-opt-in sent to `hello+launch-qa-newsletter-20260923@namicreative.co.uk` was confirmed, and Mailchimp readback showed `subscribed` with the expected `Subscriber` and `source:newsletter` tags. No real member was contacted.

The Make history audit found no unresolved failures across Contact, Creative Network, Profile Pictures, Public Directory Feed, Weekly Featured, Newsletter Welcome and the locked Directory Invite Campaign. Two older isolated errors were followed by successful executions and are recorded as resolved.

Staging no longer inherits the production cron schedule. Staging version `a88946bc-9f10-4edd-bf94-68978a1d478c` has no cron entries and passed the 21-check safety suite after deployment.

A pending Cloudflare Free DNS zone was prepared without changing live delegation. All 11 Netlify DNS records were inventoried, email-related CNAME records are DNS-only, and Cloudflare assigned `dawn.ns.cloudflare.com` and `kenneth.ns.cloudflare.com`. The current GoDaddy delegation remains on Netlify's four NS1 nameservers. The imported Netlify origins were tested directly: apex returned 200 and `www` returned the expected 301 on both origins. See `docs/cloudflare-cutover-runbook.md` for the exact cutover and rollback sequence.

## Google Cloud ownership correction (2026-09-22)

The previous staging Sheet service account belonged to the unrelated WPG Google Cloud project. Joe created the NAMI-only project `nami-creative-network`, enabled `sheets.googleapis.com`, and created `nami-network-sheet-sync@nami-creative-network.iam.gserviceaccount.com` without project IAM roles. The new account has `writer` access only to the `NAMI Feature Submissions` spreadsheet, confirmed by Drive permission readback. Staging now uses this NAMI account. Joe removed the old WPG service account from the NAMI Sheet; Drive permission readback on 2026-09-22 confirms it is absent while the new NAMI account remains a writer. WPG's unrelated resources were not modified.

The NAMI-only JSON key was created and its project ID, service-account email, and private-key presence were verified locally without displaying the key. The private key was installed as a Cloudflare staging secret and staging Worker version `9dd6591f-c4de-4d67-baab-a5cda51bbb67` now uses the NAMI service-account email. A staging-only QA sync job `launch-qa-nami-project-20260922` completed with zero retries or errors. Readback of `Creative Network Staging!A4:AA4` found the same member ID, a later successful-sync timestamp, and `Synced` status. The 21-route staging smoke check still passes. The key was also uploaded to production as *unpublished* Worker version `1ee0c2de-0984-4311-b862-ab214eb5c04c`; deployment history confirms no production Worker activation on 2026-09-22. The JSON download remains outside the repository pending secure cleanup after promotion.

Cutover caution: the production key is in an unpublished Worker version, not the active deployment. The final production code upload must include or preserve this secret and verify its binding before enabling production Sheet jobs. Do not assume a later `wrangler deploy` automatically promotes an unpublished secret version.

Read-only recheck, 2026-09-22: the local typecheck and Cloudflare build exited 0; staging and the isolated production-data preview each passed 21/21 route and hold-gate checks; 17/17 sampled live-versus-staging route statuses matched; the Make configuration audit passed 18/18 checks; all 93 staging profiles with stored image keys had accessible images; and the production-data preview passed 185/185 published profile pages. Production D1 still has exactly two pending Sheet jobs (`migration-profile-image-ellie` and `migration-profile-image-joe`), zero email jobs and zero member invitations. Apex and `www` still resolve to the recorded Netlify IPs. The browser, delivery and member-edit checks that were still open here were completed in the 2026-09-23 pass above.

Production Netlify, production DNS and member invitations remain unchanged until this register is signed off. Use staging test identities and mark each check with a date, result and evidence link. `Not run` is not a pass.

| Check | Result | Evidence needed |
| --- | --- | --- |
| Local typecheck and Cloudflare production build | Pass, reconfirmed 2026-09-23 | `npm.cmd run typecheck` and the latest `npm.cmd run build:cloudflare` exited 0. The final production candidate is unpublished version `d52258d9-fbc8-4b57-a671-85f3ce5aafc3`; staging is version `a88946bc-9f10-4edd-bf94-68978a1d478c`. Rebuild after any further code changes. |
| Public route, invalid-input, access-control and integration-hold smoke check | Pass on staging and exact production version, reconfirmed 2026-09-23 | `node scripts/prelaunch-readonly-check.mjs`: 21/21 expected statuses on both targets. News returns 404, held integrations return 503 and protected APIs reject unauthenticated access. |
| Live-versus-staging route status parity | Pass, reconfirmed 2026-09-22 | `node scripts/prelaunch-route-parity.mjs`: 17/17 sampled routes match status, including News returning 404 on both sites. This compares statuses, not full page content. |
| Desktop and mobile layout at 360, 390, 430 and 768 px | Pass, reconfirmed 2026-09-23 | Home, Contact, Creator Wave, Network, Directory, Login, Joe's profile and Joe's authenticated dashboard were checked at 360, 390, 430, 768 and 1440 px. No document overflow was found; visible content and form controls retained side padding. Hidden honeypot fields and fixed decorative cursor elements are intentionally offscreen. |
| Contact form | Pass, 2026-09-23 | The staging route and Make execution succeeded, ordinary requests remained held, and Joe confirmed the controlled notification reached the owner mailbox. No real member address was used. |
| Creator Wave workshop form | Pass, 2026-09-23 | The staging route and Make execution succeeded, and Joe confirmed the controlled notification reached the owner mailbox. No real member address was used. |
| Newsletter signup | Pass, 2026-09-23 | The staging route accepted owner-controlled address `hello+launch-qa-newsletter-20260923@namicreative.co.uk`, Mailchimp sent double opt-in, Joe confirmed it, and API readback showed `subscribed` with the expected tags. No real member was contacted. |
| Creative Network application | Pass, 2026-09-23 | The controlled application was saved, approved to one card and page, synchronized through the NAMI-only Google credential, and Joe confirmed its owner notification reached the mailbox. No invitation was requested and no real member was contacted. |
| Profile-picture workflow | Pass, 2026-09-23 | All 93 stored staging images remained accessible. The authenticated member test rejected an invalid upload, accepted a valid profile image and portfolio image, served the resulting WebP publicly, and deleted the portfolio image. Profiles without a supplied image continue to show the intended placeholder. |
| Every other Make scenario that reads or writes site data | Pass for prelaunch history and configuration audit, 2026-09-23 | Configuration checks passed. The read-only history audit found no unresolved failures across all seven relevant scenarios; two older isolated errors were followed by successful executions. The locked invitation scenario remains on-demand and was not invoked. |
| Approval creates one public card and one stable profile page | Pass, 2026-09-21; Sheet readback 2026-09-22 | `node scripts/prelaunch-member-journey.mjs`: owner-approved staging test member `launch-qa-77d36890-849` is published, has an unclaimed account, directory link, profile URL, WebP image, OG image and JSON-LD schema. The staging Google Sheet has exactly one matching Member ID row, with `Synced` status and a last-successful-sync timestamp. |
| Approval sends no invitation and creates no invitation token | Pass, 2026-09-21 | Read-only staging D1 query for test member: `member_invites=0`, `email_jobs=0`, `sheet_sync_jobs=1`. |
| Production-to-staging member ID comparison | Pass for launch data selection, reconfirmed 2026-09-23 | Production remains the launch source: 185 member IDs, 185 published profiles and 95 stored image keys. Staging has 184 synthetic/migration-era records and is not promoted as a database. The production-only member pages pass on the exact production Worker candidate. |
| Production-data Cloudflare preview | Pass for public/read-only paths, 2026-09-23 | The isolated preview and exact unpublished production version use production D1 and R2 with all outbound switches held. Each passes 21/21 route checks, and the exact version passes 185/185 published profile pages including schema, OG image and private-member-email checks. |
| Production Worker secret parity | Pass for held launch, 2026-09-23 | Exact-version inspection confirms the production candidate has the NAMI Google key, Graph credential, Turnstile, Make and Mailchimp secrets plus all launch holds. Graph account-email delivery remains deliberately untested and must stay held until its separate postlaunch test. |
| Pending production scheduled jobs | Pass with cutover action recorded, 2026-09-23 | Production D1 has two known Sheet-sync jobs for `ellie-grassick` and `joe-wilson-nami-creative`, zero email jobs and zero member invitations. Both target the intended production Sheet records and will be watched when the production cron is activated. |
| Member dashboard profile edit | Pass, 2026-09-23 | Login, dashboard load, profile read, invalid and valid saves, image operations, audit rows, public page, directory card and successful Sheet readback all passed with the synthetic staging account. |
| Backout to Netlify | Pass for documented fallback, 2026-09-23 | Current delegation and all 11 Netlify records are recorded. Cloudflare's two imported Netlify origins returned the correct apex and `www` responses. The launch will use Worker routes over retained Netlify records, so removing the routes is the immediate rollback. The nameserver reversal is recorded in `docs/cloudflare-cutover-runbook.md`; it was not performed because that would change the live site. |

## Make scenario inventory

The site code currently calls Make from the contact form, Creator Wave workshop form, Creative Network application and profile-picture workflow. This is a code-path inventory, not proof that every Make scenario has been found or tested. Inspect the Make account for downstream scenarios and any scheduled or inbound flows before sign-off. Use only test addresses and clearly named test records. Keep `EXTERNAL_INTEGRATIONS_MODE=hold` until each destination is ready for controlled staging runs.

The contact and workshop routes count missing credentials or non-2xx integration responses as failed pathways. Contact success requires Make or the owner dashboard to receive the brief; a Mailchimp-only copy is not enough. Staging has the Make and Mailchimp secret names, but lacks `DASHBOARD_LEAD_WEBHOOK_URL` and `DASHBOARD_LEAD_WEBHOOK_SECRET`. The owner-dashboard repository currently has no matching lead webhook route. The Make routes, scenario executions and owner-mailbox delivery have now been verified with controlled test data. `EXTERNAL_INTEGRATIONS_MODE` remains held for ordinary staging traffic.

The local `MAKE_API_TOKEN` was verified through Make's EU2 read-only API on 2026-09-21. It exposed 27 scenarios in the Nami Creative team. All four webhook URLs in `.env.local` match their intended active Make scenarios. Contact and Creative Network send Microsoft email to the owner address; Weekly Featured Member also sends to the owner. Newsletter Welcome and Directory Invite Campaign [LOCKED] map recipients dynamically and therefore must not be invoked with real member data during launch tests. The invite campaign is on-demand, not scheduled. This inspection confirms configuration only, not a successful execution or delivery. No hook was called and no email was sent.

On 2026-09-23, the repeatable configuration audit passed and the new history audit reviewed Contact, Creative Network, Profile Pictures, Public Directory Feed, Weekly Featured, Newsletter Welcome and Directory Invite Campaign [LOCKED]. It found no unresolved failures. Creative Network had one older Google Sheets error on 2026-09-08 and the locked invitation campaign had one older Microsoft email error on 2026-09-04; each was followed by a successful execution. The two Creative Network Sheet writes target different spreadsheets and both map the permanent member ID. No scenario was changed by this audit.

A separate controlled contact webhook test on 2026-09-22 sent one clearly marked payload using `launch-qa@example.invalid`; it returned 200 and the next Make execution succeeded. On 2026-09-23, Joe confirmed the controlled Contact, Creator Wave and Creative Network notifications reached the owner mailbox. No real member was addressed.

The owner dashboard's `NETWORK_DB` binding points to production `nami-network`, while the public staging Worker points to `nami-network-staging`. The staging approval API was tested directly. The protected owner-dashboard UI is not required for the initial held launch and remains part of the postlaunch owner-notification work.

## Invitation gate

Build verification, 2026-09-23: the latest `npm.cmd run typecheck` and `npm.cmd run build:cloudflare` exited 0. Staging Worker version `a88946bc-9f10-4edd-bf94-68978a1d478c` has no cron schedules and contains the Sheet reconciliation, QA form gate and profile-link normalization fixes. `node scripts/prelaunch-readonly-check.mjs` passed 21/21 against this exact deployment. Ordinary submissions still stop at the external-integration hold; News still returns 404. The production Worker deployment and live DNS were not changed.

Sheet reconciliation update, 2026-09-23: Make maps an application's permanent ID into the legacy `Directory ID` column on the main Creative Network tab. The newer `Member ID` column is blank before approval. Approval sync searches both columns and rejects duplicate or conflicting matches; six local tests pass. The controlled staging member completed four queued Sheet updates, and direct readback found exactly one matching row with the final dashboard edit, canonical Instagram URL, image key and successful sync status.

Staging approval update, 2026-09-22: `node scripts/prelaunch-approve-make-test.mjs` approved only `launch-qa-make-20260922`, the application submitted through the real Make scenario. It is published with an unclaimed account, a public directory card, a 200 profile page, OG image and schema. No invitation was requested. The staging-only Sheet processor reported one completed job and zero failures. Direct Sheet readback found exactly one matching row in `Creative Network Staging` (row 4), with status `Synced` and a successful sync timestamp. The main production Sheet row 190 is intentionally untouched by staging approval.

Live-directory QA cleanup, 2026-09-22: the Netlify site's Public Directory Feed Make scenario reads the separate `NAMI Creative Network Public Directory Feed` spreadsheet, `Directory` tab, and includes only rows with a populated `Directory ID` (column J). The exact QA entry `launch-qa-make-20260922` was row 191. Its `J191` value alone was cleared, leaving the rest of that row and the original application in `NAMI Feature Submissions` intact. Sheet readback confirmed `J191` blank; the Make feed returned zero QA matches. After Netlify's caches refreshed, `/network/directory`, `/network/directory/all`, and `/network/directory/artists` no longer included `Launch QA Make test`, and `/network/directory/member/launch-qa-make-20260922` returned 404. The staging QA member remains for launch testing. Do not repopulate this live feed ID during later QA or cutover.

`MEMBER_INVITATIONS_MODE` and `OUTBOUND_EMAIL_MODE` must both remain `hold`. Approving an application publishes a profile without creating an invitation. Enabling either mode alone must not invite anyone. Review old held email jobs and expired tokens before any later invitation campaign. Member invitations require a separate final approval after Cloudflare cutover, form/Make checks, page creation, mobile checks and the planned profile improvements.

## Post-launch owner notifications and tickets

After the site is live and the launch checks pass, build an owner ticket log and send `hello@namicreative.co.uk` an email for each new Creative Network application with a link to its review screen in the Cloudflare Access-protected owner dashboard. The link opens the application; approval still requires an authenticated action. Send new error tickets to the same mailbox, including the ticket ID and a link to its owner-dashboard record. These owner-only alerts must have a separate enable switch and recipient allowlist so turning them on cannot release held member invitations or account email. Verify the actual dashboard URL and an end-to-end test with Joe's address before enabling alerts. These new alerts remain disabled during prelaunch checks; the one controlled owner contact notification is recorded above. No member email is authorised.

Redesign the newsletter welcome email after launch. The current version uses the old NAMI services-led messaging and reads as AI-written. Replace it with concise, current NAMI copy in Joe's familiar, northern and supportive voice, then test the revised automation with an owner-controlled address before publishing it.

Network News stays offline until Joe has populated and approved it. Its index and article URLs return 404 on staging. Read-only HTML checks found no News URL in the sitemap or directory links.

## Cutover and backout record

Historical pre-cutover note: before the switch, the live site returned `Server: Netlify`. The Netlify deployment and recorded DNS configuration remain available as the documented rollback path. Production traffic now runs through Cloudflare as recorded in the live validation section above.

The planned switch uses Worker routes over retained proxied Netlify records. Removing the two routes restores Netlify immediately without waiting for another nameserver change. The GoDaddy nameserver reversal is documented as the second-level fallback. Keep the Netlify deployment, DNS zone and domain configuration intact for at least 14 days after cutover.

## Background monitoring

Hermes Agent v0.21.0 is installed locally. `hermes verify --detect-only --json .` confirmed the Next.js build and typecheck recipe on 2026-09-22. Its gateway is not running and no cron job is active, so no unattended checks are occurring. A full Hermes status check also hit home-directory permission errors in this execution sandbox. It may run the read-only smoke and Make audit scripts once an isolated script-only schedule and notification destination are working. It must not submit valid forms, approve members, change DNS, deploy code, access secrets or send email. The deterministic scripts remain the source of pass/fail.
